import { createStore } from 'vuex'
import tasksModule from './modules/tasks'

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    tasks: tasksModule,
  }
})
